---
import { formatFullDate } from 'src/lib/content';
import { t, readingTime } from 'src/locales';

const { content } = Astro.props;
const { title, pubDate, updatedDate, description, heroImage, minutesRead } = content;
---
<article class="post">
    {heroImage && (
        <img class="post__hero-image" src={heroImage} alt={`${t('image')}: ${description}`} />
    )}
    <h1 class="post__title">{title}</h1>
    <div class="post__meta">
        {pubDate && <time datetime={pubDate}>{formatFullDate(pubDate)}</time>}
        {updatedDate && <>• ({t('updated')} <time datetime={updatedDate}>{formatFullDate(updatedDate)}</time>)</>}
        •
        {readingTime(minutesRead)}
    </div>
    <div class="post__content">
        <slot />
    </div>
</article>
